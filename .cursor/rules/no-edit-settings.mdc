---
description: Запрет изменения .cursor/settings.json агентом
alwaysApply: true
---

# Запрет изменения settings.json

**Нельзя** изменять, удалять или переименовывать:

- `.cursor/settings.json`

Этот файл задаёт политику автономии агента. Менять его может только пользователь.

Допустимо только **чтение** для сверки с политикой.
