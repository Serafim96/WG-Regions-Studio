# Журнал изменений

Все заметные изменения [WG Regions Studio](https://github.com/Serafim96/WG-Regions-Studio) фиксируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/).

English version: [CHANGELOG.md](../EN/CHANGELOG.md)

## [2.0.15] — 2026-08-11

### Изменено

- Эталонный YAML для полного набора тестов (`wg_regions_reference.yml`) больше не содержит реальных имён регионов, мировых координат и UUID владельцев — детерминированные синтетические id / сдвиг координат / uuid5 (топология и флаги сохранены).
- Справочник spatial-пар — в `backend/tests/fixtures/`; устаревший `data/reference_spatial_edges.json` удалён.

## [2.0.14] — 2026-08-10

### Добавлено

- На пустой схеме сразу после запуска: кнопка **+** и **ПКМ** → добавить временный регион (без открытия файла); после добавления кнопки сайдбара работают как при обычной схеме.

### Изменено

- **Сбросить схему**, если остались только временные регионы (или удаление последнего региона) — возврат к состоянию сразу после запуска.

### Исправлено

- При одном узле на схеме начальный fit / zoom-floor больше не ломают общий предел приближения (1.35).

## [2.0.13] — 2026-08-10

### Исправлено

- **Zoom колёсиком** на схеме больше не «прыгает» с разной скоростью после загрузки схемы (вместо автоопределения устройства Cytoscape — фиксированный шаг на щелчок).
- Ориентация кончика стрелки у иконки **обновления** в меню уведомлений.

## [2.0.12] — 2026-08-10

### Добавлено

- Окно **«Список изменений»** с полной историей релизов (новые сверху, с датами); открывается при первом запуске новой локальной сборки (офлайн: локальная версия + id фронтенд-бандла в памяти браузера, без GitHub) и в любой момент по кнопке-книге рядом с переключателем темы.
- Полный экран: горячая клавиша **F** (не срабатывает при вводе в поле) и закреплённая кнопка внизу сайдбара.
- Ошибки, блокирующие экспорт (некорректные id регионов, циклы parent, неполные временные регионы), показываются в колокольчике как **error**; при блокировке экспорта — короткая вспышка у кнопки «Экспорт YAML».

### Изменено

- Минимальная / начальная ширина сайдбара — 320px; язык, журнал и тема — в одной строке настроек.
- В упакованную сборку входят `docs/*/CHANGELOG*.md` для истории в приложении; `/api/version` отдаёт двуязычный changelog.
- Скрипт сборки Windows не переустанавливает PyInstaller/Pillow, если они уже есть (`/force-deps` — принудительно).

## [2.0.11] — 2026-08-10

### Изменено

- Windows-лаунчер из исходников: брендированное окно classic `conhost` открывается **до** установки зависимостей — вывод pip/npm сразу под иконкой приложения.
- После установки сервер остаётся в том же окне консоли (без второго relaunch).

## [2.0.10] — 2026-08-09

### Добавлено

- Окно **«Что нового»** при первом запуске новой версии (один раз на сборку).
- В уведомлении об обновлении — краткий список изменений из описания релиза на GitHub.
- В заголовке README — иконка приложения (`packaging/icon.png`).

### Исправлено

- Упакованный Windows EXE: нет краткой вспышки консоли при старте (Windows subsystem + AllocConsole в процессе).

## [2.0.9] — 2026-08-09

### Изменено

- Windows: сервер всегда перезапускается в классическом `conhost` (в том числе при двойном клике по `.exe`, если Default Terminal = Windows Terminal) — отдельное окно консоли с иконкой приложения, не вкладка Windows Terminal.
- Во вкладке браузера — favicon приложения.

### Исправлено

- Двойной клик по `WG-Regions-Studio.exe` больше не открывает процесс во вкладке Windows Terminal при системном Default Terminal = WT.

## [2.0.8] — 2026-08-09

### Изменено

- Иконка приложения: доработаны ромашка и **WGS** (размер/интервалы букв, толщина штриха); Windows `.exe` / zip пересобраны с новой иконкой.

## [2.0.7] — 2026-08-09

### Добавлено

- Готовая Windows-сборка: `WG-Regions-Studio.exe` (иконка ромашка + **WGS**) и вспомогательные файлы в zip (`packaging\build_windows.bat`).
- One-click лаунчеры: `WG-Regions-Studio.bat` (Windows), `.command` / `.app` (macOS), `.sh` (Linux) — проверка/установка зависимостей и запуск (`launch.py`).

### Изменено

- Удалён `run.bat`; `setup.*` / `run.sh` — обёртки над новыми лаунчерами.

## [2.0.6] — 2026-08-09

### Изменено

- Бейдж выбранного региона справа сверху: **«Регион: …»** (было «Выбрано: …»).

## [2.0.5] — 2026-08-09

### Добавлено

- Рядом со справкой по флагу (**?**) в карточке региона / ведении флагов — кнопка-иконка «показать на схеме флага»; при несохранённых правках — короткое предупреждение сохранить сначала.
- На схеме флага справа сверху — бейдж **«Флаг: …»** (клик по имени — подогнать камеру; слева — справка и ведение флагов с фильтром).
- Легенда карты: вкладки **«Схема»** / **«Схема флага»** с образцами подписей узлов (◆ / ◇ / ∈ / ≈ и свечение текста).

### Изменено

- Затемнение на схеме флага — блёклые непрозрачные узлы вместо низкой opacity (рёбра не просвечивают).
- Spatial-рёбра на схеме флага: подсвеченные пересечения/вхождения — сплошные; dim-пересечения — блёклый пунктир; меньше дорогих комбинаций пунктир+прозрачность.
- Свечение текста (`text-outline`) только у регионов, где флаг задан (зелёное), и у участников конфликта (красное).
- Шире запас пана камеры вокруг схемы; диалог выбора флага всегда открывается с пустым полем.
- Сайдбар при старте развёрнут; пределы зума/пана включены.

## [2.0.4] — 2026-08-08

### Изменено

- Название в сайдбаре **WG Regions Studio** не переводится при русском интерфейсе.

## [2.0.3] — 2026-08-08

### Изменено

- Переименование продукта: **Студия Регионов WG** / **WG Regions Studio** (репозиторий: [Serafim96/WG-Regions-Studio](https://github.com/Serafim96/WG-Regions-Studio)).

## [2.0.2] — 2026-08-08

### Добавлено

- **Проверка обновлений** — при запуске сверка версии с последним релизом на GitHub; при устаревшей версии — toast и предупреждение в колокольчике со ссылкой на страницу релиза.

## [2.0.1] — 2026-08-08

### Добавлено

- **Очистить флаги** — жёлтая кнопка в карточке региона и в ведении флагов (выбранный регион), с подтверждением да/нет; очищает черновик, затем **«Сохранить»**.
- **Удалить все флаги** — красная кнопка в ведении флагов: снимает все флаги со всех регионов схемы, с подтверждением.
- Backend `DELETE /api/regions/flags` для очистки всех флагов регионов в сессии.
- **Уровень вложенности** — неизменяемое поле в карточке региона (как `d:` на схеме).

### Изменено

- **Ведение флагов** — убрана вкладка «дерево флага»; при фильтре по флагу рядом с регионом показывается значение, галка **«Наследование»** показывает унаследованные регионы.
- Подсветка флага: у унаследованных узлов светлый текст с обводкой — читается на цветной заливке (в т.ч. фиолетовой на уровне 2).

## [2.0.0] — 2026-08-08

### Добавлено

- **Открыть файл** — одна кнопка в сайдбаре открывает WorldGuard `regions.yml` / `.yaml` или сохранённую схему `.mrv.json`; из YAML схема строится сразу.
- **Экспорт YAML** — скачивание совместимого с WorldGuard `regions.export.yml` из текущей сессии, с вопросом про временные регионы и прокручиваемым диалогом ошибок (некорректные имена, циклы parent, неоднозначные конфликты флагов, нет координат).
- **Конфликты флагов** — анализ перезаписей по наследованию и пространственных пересечений; показ пар на схеме; неоднозначные случаи с равным приоритетом — ошибки в колокольчике уведомлений.
- **Колокольчик уведомлений** — ошибки и предупреждения (конфликты, сироты, перезаписи); клик по пункту фокусирует на схеме; иконка обновления в заголовке пересобирает список без toast.
- **Временные регионы** — черновики (включая потомков), правка геометрии/флагов/участников, переименование и удаление любого региона (каскад / переназначение родителя / сирота).
- **Ведение флагов** — редактор дерева, массовые операции, справочник; фильтр по флагу со значением и галкой наследования; кнопка флага на схеме — поиск + **Отобразить**; опции подсветки (пересечения / вхождения / наследование / конфликты) с метками ∈ / ≈ и значениями носителей.
- **Пересечения в карточке региона** — таблица частичных пересечений с **затронутыми блоками** и **процентом**, сортировка; в рёбрах хранится `overlapBlocks` после построения.
- **Инструменты схемы** — сброс схемы (убрать временные и пересобрать), очистка сессии, режимы рёбер, подсветка ветки / вхождения / пересечения, выравнивание раскладки, блокировка узлов.
- **UI** — тёмная/светлая тема, RU/EN, диалоги подтверждения в приложении (без браузерного `confirm()` для основных сценариев), зелёное сохранение / красное удаление, естественная сортировка в списках.

### Изменено

- Убран отдельный поток **Открыть YAML** / **Открыть схему** / **Построить схему** / **Отменить загрузку YAML** / **Перестроить схему**.
- Диалог выбора файла только с расширениями YAML и схемы (`excludeAcceptAllOption`); неверный или битый файл — диалог ошибки в приложении.
- Сайдбар больше не выводит счётчики узлов / авто-сворачивания в строку статуса (см. **Метрики**).
- Пустая схема: **Сохранить** и действия ниже неактивны, пока схема не загружена.
- Подписи вхождения в карточке сокращены до **Внутри** / **Содержит**; на схеме флага метка вхождения — **∈** (со значением контейнера) вместо ∉.
- Подсветка флага: понятнее задано vs унаследовано; парковка сирот и разведение пересечений в раскладке; в режиме «только конфликты» сохраняются нужные пути наследования.
- Кнопка сворачивания сайдбара остаётся слева сверху на схеме (рядом с добавлением временного региона), а не внутри панели.
- ПКМ по пустому месту схемы — меню добавления временного региона; при добавлении с узла родитель подставляется, но остаётся редактируемым.

### Документация

- README и двуязычные руководства в `docs/EN/` и `docs/RU/` обновлены под текущий UI.

## [0.1.0] — 2026-08 — первая публичная версия

Базовая публикация на GitHub: разбор YAML → иерархия + пространственные рёбра, схема Cytoscape, сворачивание/поиск/легенда/метрики, сохранение/загрузка схемы (`.mrv.json`), двуязычный UI, pytest, локальные `regions.yml` / `all_flags.txt`.

[2.0.8]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.8
[2.0.7]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.7
[2.0.6]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.6
[2.0.5]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.5
[2.0.4]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.4
[2.0.3]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.3
[2.0.2]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.2
[2.0.1]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.1
[2.0.0]: https://github.com/Serafim96/WG-Regions-Studio/releases/tag/v2.0.0
[0.1.0]: https://github.com/Serafim96/WG-Regions-Studio/tree/6e5bbc9
